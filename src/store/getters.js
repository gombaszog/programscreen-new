export default {
    programData(state) {
        return state.programData
    },

    actualPrograms(state) {
        return state.actualPrograms
    },

    actualLocations(state) {
        return state.actualLocations
    },

    screenStep(state) {
        return state.screenStep
    },

    skippedScreens(state) {
        return state.skippedScreens
    }
}
